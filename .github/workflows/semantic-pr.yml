name: semantic-pr

permissions:
  pull-requests: read

jobs:
  semantic-pr:
    name: semantic-pr
    runs-on: ubuntu-latest
    steps:
      - uses: amannn/action-semantic-pull-request@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
